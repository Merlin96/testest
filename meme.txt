PROVERKA SVYAZI

SVYAZ' ustanovlena

ima watch git gui work

Git использует SHA1 для создания хэша файлов, который представляет уникальный код каждого объекта.

В папке .git хранятся важные файлы и директории, такие как Objects, где находятся коммиты, и HEAD, который указывает на последний коммит.

Объекты в Git: три типа — коммит, дерево и BLOB (Binary Large Object). Коммиты ссылаются на деревья, которые хранят информацию о названиях файлов и путях, а BLOB содержит содержимое файла. commit(ссылка)>tree(Название и путь)>BLOB(Вся информация)

new memes 13 10

added feature meme 1310

trying to merge